/// @description Variable initialization.

// Inherit the parent event
event_inherited();

mouth_x = 0; // This is an offset, not the actual position.
mouth_y = 0;

name = noone;

voice = noone; // TODO: add default voice.
voice_fast = noone;

font = fnt_dialogue;
